# OS.js WebSocket Application Example

This was implemented using `ws.js`, which seems to be a good choice.

## Setup

Firstly do this in the package directory:

```
$ npm install
```

Then proceed as normal
